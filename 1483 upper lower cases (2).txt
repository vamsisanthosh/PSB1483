// Online C compiler to run C program online
#include <stdio.h>
#include <string.h>
int main() 
{
    char U,L;
    int ascii;
    printf("enter the upper case string");
    scanf("%C",&U);
    ascii=U+32;
    printf("%c character in lower string is :%c",U,ascii);
    return 0;
}